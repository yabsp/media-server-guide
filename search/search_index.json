{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Media Server Guide","text":"<p>Setting up a proper Linux server to host a media library can be quite time-consuming \u2014 even when using modern tools. This is why I created this guide.</p>"},{"location":"#how-to-read-the-guide","title":"How to Read the Guide?","text":"<p>The guide is structured in chapters and sections. The chapters are ordered and the order of the chapters and sections within each chapter is exactly the order in which the steps for the setup should be executed.</p>"},{"location":"#arr-apps-and-others","title":"Arr Apps and others","text":"<p>Configuration guides for Sonarr, Radarr, Seerr, NZBGet, Unpackerr and Plex in detail are not provided yet. Since user preferences differ for these tools, priority is rather low for these guides \u2014 nonetheless this is planned for future updates. Further reading material is provided in Further Links &amp; Related Guides.</p>"},{"location":"#work-in-progress","title":"Work in Progress","text":"<p>This documentation is by no means final and can always be improved further. Security has been taken into consideration, but there might still be risks and issues with this setup. You are expected to think for yourself about why we do each step and how to do it properly.</p>"},{"location":"#legal-notice","title":"Legal Notice","text":"<p>Please keep in mind that downloading copyright protected material is not legal in every region. This guide is specifically written to follow the Federal Act on Copyright and Related Rights of Switzerland.</p>"},{"location":"01-debian/","title":"Base System Installation","text":""},{"location":"01-debian/#operating-system-setup","title":"Operating System Setup","text":""},{"location":"01-debian/#installation-philosophy","title":"Installation Philosophy","text":"<p>The goal is a headless server to minimize resource usage.</p> <ul> <li>OS: Debian 13 (Trixie), but basically any Linux OS will do here.</li> <li> <p>GUI: None (No GNOME/KDE). Terminal only. Optionally you can install a desktop (e.g. GNOME) but keep it deactivated unless you actually need it.</p> <pre><code>sudo systemctl set-default multi-user.target\n</code></pre> </li> <li> <p>Filesystem: ext4.</p> </li> </ul>"},{"location":"01-debian/#root-password-strategy","title":"\"Root\" Password Strategy","text":"<p>During the installation process, Debian asks for a <code>root</code> password.</p> <ul> <li>Action: We leave the root password field empty.</li> <li>Reasoning: This forces Debian to disable the direct root account login and automatically adds the primary user to the <code>sudo</code> group. This is easier and arguably safer than having a user with the same password as root.</li> </ul>"},{"location":"01-debian/#software-selection","title":"Software Selection","text":"<p>In the tasksel menu (blue screen near the end), we select:</p> <ol> <li>SSH Server (Crucial for remote access), if you did not select this refer to User &amp; Network for the SSH server installation.</li> <li>Standard System Utilities</li> </ol>"},{"location":"02-network/","title":"Initial Configuration","text":""},{"location":"02-network/#user-management","title":"User Management","text":"<p>Since we skipped the root password, our created user (let's call him <code>joachim</code> or similar) has sudo rights.</p>"},{"location":"02-network/#verifying-sudo-access","title":"Verifying Sudo Access","text":"<p>To verify that the user can perform administrative tasks:</p> <pre><code># Check if user belongs to sudo group \ngroups\n\n# Test sudo access (should ask for user password) \nsudo apt update\n</code></pre>"},{"location":"02-network/#setting-the-hostname","title":"Setting the Hostname","text":"<p>To ensure the server is easily identifiable on the network:</p> <pre><code>sudo hostnamectl set-hostname mediaserver\n</code></pre>"},{"location":"02-network/#dedicated-service-user-setup","title":"Dedicated Service User Setup","text":"<p>For security and clean permission management, we do not run the applications as the administrative user. Instead, we create a dedicated system user (named <code>media</code>) for all Docker containers.</p>"},{"location":"02-network/#creating-the-media-user","title":"Creating the Media User","text":"<p>We create a system user with a specific UID and disable login capabilities for security.</p> <pre><code># Create user 'media' with UID 13000, create matching group, no login, no home\nsudo useradd -u 13000 -U -M -s /bin/false media\n\n# Verify creation\nid media\n# Output: uid=13000(media) gid=13000(media) groups=13000(media)\n</code></pre> <ul> <li> <p>PUID: 13000</p> </li> <li> <p>PGID: 13000</p> </li> <li> <p><code>-U</code>: Creates a group with the same name and ID.</p> </li> <li> <p><code>-M</code>: Do not create a home directory.</p> </li> <li> <p><code>-s /bin/false</code>: Disable login.</p> </li> </ul>"},{"location":"02-network/#granting-access-to-the-admin-user","title":"Granting Access to the Admin User","text":"<p>To ensure that we (the logged-in admin) can still manually manage files in the storage pool, we add our current user to the new group.</p> <pre><code>sudo usermod -a -G media joachim\n</code></pre>"},{"location":"02-network/#applying-filesystem-permissions","title":"Applying Filesystem Permissions","text":"<p>We transfer ownership of the data directory to the new user and group. We set permissions to <code>775</code>, allowing full access to both the service user and group members (us).</p> <pre><code># Change ownership to media:media \nsudo chown -R media:media /mnt/data\n\n# Set permissions (User=RWX, Group=RWX, Others=RX) \nsudo chmod -R 775 /mnt/data/\n</code></pre> <p>NB: r = 4, w = 2, x = 1</p>"},{"location":"02-network/#enforcing-read-only-access-for-plex","title":"Enforcing Read-Only Access for Plex","text":"<p>While services like Sonarr need write access to manage files, Plex should not modify media files.</p> <p>Instead of complex filesystem ACLs, we will handle this at the Docker level later.</p> <ul> <li> <p>All containers run as <code>PUID=13000</code>.</p> </li> <li> <p>Sonarr/Radarr mount volumes as Read-Write.</p> </li> <li> <p>Plex mounts the media volume with the <code>:ro</code> (Read-Only) flag in Docker     Compose (see Docker &amp; Services).</p> </li> </ul>"},{"location":"02-network/#network-configuration","title":"Network Configuration","text":"<p>Since this is a server, it needs a static IP. In my case I added the following interface:</p> <p><pre><code># Edit the file /etc/network/interfaces\nsudo vim /etc/network/interfaces\n</code></pre> And add:</p> <pre><code># The primary network interface\nallow-hotplug enp4s0\niface enp4s0 inet static\n    address 192.168.0.50\n    netmask 255.255.255.0\n    gateway 192.168.0.1\n    dns-nameservers 1.1.1.1 8.8.8.8\n</code></pre> <ul> <li> <p><code>allow-hotplug</code>: Tells the system to start the network interface <code>enp4s0</code> as soon     as the kernel detects that the hardware is present. This prevents the boot     process from hanging while waiting for the interface, which can happen with     <code>auto</code>.</p> </li> <li> <p><code>inet</code>: Use IPv4 (for IPv6 use <code>inet6</code>).</p> </li> <li> <p><code>gateway</code>: IP address of the router.</p> </li> </ul> <p>Following commands are useful:</p> <pre><code># 1. Find the interface name (e.g., eth0, enp4s0, eno1) \nip a\n\n# 2. Find the gateway IP (look for the 'default via ...' line) \nip r\n</code></pre>"},{"location":"02-network/#language-locale","title":"Language &amp; Locale","text":"<p>We set the system language to English but kept the keyboard layout Swiss (or German) as I am using a Swiss keyboard.</p> <pre><code># Reconfigure Locales (Set to en_US.UTF-8)\nsudo dpkg-reconfigure locales\n\n# Check Keyboard layout (Should be 'ch' or 'de') \ncat /etc/default/keyboard\n</code></pre>"},{"location":"02-network/#ssh-server-installation","title":"SSH Server Installation","text":"<p>If the SSH server was not selected during the initial OS installation, it must be installed manually.</p>"},{"location":"02-network/#installation-via-apt","title":"Installation via APT","text":"<pre><code># 1. Install the package \nsudo apt update \nsudo apt install openssh-server\n\n# 2. Enable and start the service \nsudo systemctl enable --now ssh\n\n# 3. Verify the status \nsudo systemctl status ssh\n</code></pre>"},{"location":"02-network/#connecting-to-the-server","title":"Connecting to the Server","text":"<p>From a remote machine (Windows PowerShell or Linux Terminal):</p> <pre><code># Syntax: ssh username@ip-address \nssh user@192.168.0.50\n</code></pre>"},{"location":"02-network/#ssh-key-authentication-passwordless","title":"SSH Key Authentication (Passwordless)","text":"<p>To increase security and convenience, we configure SSH Key Authentication. This allows logging in without typing a password, using a cryptographic key pair instead.</p>"},{"location":"02-network/#1-generating-a-key-pair-on-client","title":"1. Generating a Key Pair (On Client)","text":"<p>Note: These commands are to be executed on your Laptop/PC. We use the <code>ed25519</code> algorithm.</p> <pre><code># Run this on your local machine \nssh-keygen -t ed25519 -C \"my-laptop\"\n\n# Press Enter to accept the default file location.\n</code></pre>"},{"location":"02-network/#2-copying-the-public-key-to-the-server","title":"2. Copying the Public Key to the Server","text":"<p>We transfer the public part of the key to the server using <code>ssh-copy-id</code>.</p> <pre><code># Replace user and IP with your server details \nssh-copy-id -i ~/.ssh/id_ed25519.pub user@192.168.0.50\n</code></pre>"},{"location":"02-network/#3-verifying-and-hardening","title":"3. Verifying and Hardening","text":"<p>Try to log in again. You should not be asked for a password. Once verified, we disable password authentication to prevent brute-force attacks.</p> <pre><code># Open the SSH config on the SERVER \nsudo vim /etc/ssh/sshd_config\n</code></pre> <p>Find and modify the following lines:</p> <pre><code># Disable root login (Security Best Practice)\nPermitRootLogin no\n\n# Disable password authentication \nPasswordAuthentication no\nChallengeResponseAuthentication no \nUsePAM yes\n</code></pre> <p>Restart SSH to apply:</p> <pre><code>sudo systemctl restart ssh\n</code></pre>"},{"location":"02-network/#firewall-setup-ufw","title":"Firewall Setup (UFW)","text":"<p>To secure the host system, we use UFW (Uncomplicated Firewall). While Docker manages its own network rules deeply in the system (iptables), UFW is essential to protect the host services (like SSH) and serves as a second layer of defense.</p>"},{"location":"02-network/#installation-and-default-policies","title":"Installation and Default Policies","text":"<p>First, we install UFW and define the fundamental behavior: block all incoming traffic by default, but allow all outgoing traffic (so the server can download updates).</p> <pre><code># Install UFW sudo apt update \nsudo apt install ufw\n\n# Set defaults (Do not enable yet) \nsudo ufw default deny incoming \nsudo ufw default allow outgoing\n</code></pre>"},{"location":"02-network/#defining-rules","title":"Defining Rules","text":"<p>We distinguish between critical system access (SSH) and application access (Plex, Web interfaces).</p>"},{"location":"02-network/#1-securing-ssh-crucial","title":"1. Securing SSH (Crucial)","text":"<p>We limit SSH access strictly to the local home network (LAN). This prevents anyone from the outside internet from even trying to log in, even if they had the key.</p> <p>Note: Replace <code>192.168.0.0/24</code> with your actual subnet.</p> <pre><code># Allow SSH only from local LAN \nsudo ufw allow from 192.168.0.0/24 to any port 22 proto tcp\n</code></pre>"},{"location":"02-network/#2-application-ports-pre-configuration","title":"2. Application Ports (Pre-Configuration)","text":"<p>Although we haven't installed Docker yet, we will pre-configure the firewall for the planned services.</p> <p>Public Access (Internet &amp; LAN):</p> <pre><code># Plex (Streaming) - Standard Port 32400 \nsudo ufw allow 32400/tcp\n</code></pre> <p>Private Admin Access (LAN Only): Management interfaces like Sonarr, Radarr, or Portainer should not be exposed to the open internet. We restrict them to the local network.</p> <pre><code># CasaOS / Dashboard (Port 80), only if you want to use this\nsudo ufw allow from 192.168.0.0/24 to any port 80 proto tcp\n\n# Overseerr/Seerr (Request Management - Port 5055) \nsudo ufw allow from 192.168.0.0/24 to any port 5055 proto tcp\n\n# Sonarr (8989) &amp; Radarr (7878) \nsudo ufw allow from 192.168.0.0/24 to any port 8989 proto tcp \nsudo ufw allow from 192.168.0.0/24 to any port 7878 proto tcp\n\n# NZBGet / SABnzbd (6789) \nsudo ufw allow from 192.168.0.0/24 to any port 6789 proto tcp\n</code></pre> <p>Note: Since we are using Docker to deploy these tools, we don't strictly need these UFW rules; by default Docker manipulates <code>iptables</code> directly and bypasses UFW. They are listed here for completeness and for any services you run directly on the host.</p>"},{"location":"02-network/#enabling-the-firewall","title":"Enabling the Firewall","text":"<p>Warning: Ensure the SSH rule (Section SSH Server Installation) matches your current IP address, or you might lock yourself out immediately.</p> <pre><code># Enable UFW \nsudo ufw enable\n\n# Check status and numbered rules\nsudo ufw status numbered\n</code></pre>"},{"location":"03-storage/","title":"Storage Setup (MergerFS)","text":"<p>We utilize MergerFS to combine multiple physical hard drives into a single logical storage pool. Unlike RAID, this allows drives of different sizes to be mixed, and data remains readable on the individual drives if the pool fails. Speed is a minor tradeoff here, but it should be no problem for what we are planning with our server.</p>"},{"location":"03-storage/#prerequisites","title":"Prerequisites","text":"<p>We need the necessary software packages to handle the pool and different filesystems (like exFAT for migration).</p> <pre><code># Install MergerFS and filesystem tools \nsudo apt update \nsudo apt install mergerfs fuse3 exfatprogs\n</code></pre>"},{"location":"03-storage/#disk-preparation","title":"Disk Preparation","text":""},{"location":"03-storage/#identifying-drives","title":"Identifying Drives","text":"<p>Before formatting, we must identify the correct device identifiers (e.g., <code>/dev/sda</code>, <code>/dev/sdb</code>).</p> <pre><code># List block devices and their file systems \nlsblk -f\n</code></pre>"},{"location":"03-storage/#formatting-to-ext4","title":"Formatting to ext4","text":"<p>Linux works best with the <code>ext4</code> filesystem. When preparing a new drive for media storage, we use specific flags to maximize usable space.</p> <p>Warning: This command wipes all data on the target drive!</p> <pre><code># Format the drive (Replace sdX with actual identifier)\nsudo mkfs.ext4 -m 0 -L disk1 /dev/sdX\n</code></pre> <p>Parameter Explanation:</p> <ul> <li> <p><code>-m 0</code>: By default, ext4 reserves 5% of the disk space for the root     user/system services. On a 10TB drive, this would waste 500GB. Since     this is a data drive, we set the reserved blocks to 0%.</p> </li> <li> <p><code>-L label</code>: Assigns a label (name) to the partition, making it easier to     identify later.</p> </li> </ul>"},{"location":"03-storage/#mounting-drives","title":"Mounting Drives","text":"<p>We do not mount drives using their device name (e.g., <code>/dev/sdb</code>) because these names can change after a reboot. Instead, we use the UUID.</p>"},{"location":"03-storage/#getting-the-uuid","title":"Getting the UUID","text":"<pre><code># Display UUIDs for all partitions \nsudo blkid\n</code></pre> <p>Copy the UUID string (without quotes) for the next step. If you are doing this in a shell-only environment but have a mouse available, you might want to use <code>gpm</code> to copy and paste.</p>"},{"location":"03-storage/#configuring-fstab","title":"Configuring fstab","text":"<p>We edit <code>/etc/fstab</code> to mount the drives automatically at boot.</p> <pre><code># Create mount points first \nsudo mkdir -p /mnt/disk1 \nsudo mkdir -p /mnt/disk2\n\n# Edit the file system table \nsudo vim /etc/fstab\n</code></pre> <p>Add the following lines for the physical disks:</p> <pre><code># Physical Disk 1 \nUUID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx /mnt/disk1 ext4 defaults 0 2\n\n# Physical Disk 2 \nUUID=yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy /mnt/disk2 ext4 defaults 0 2\n</code></pre>"},{"location":"03-storage/#the-mergerfs-pool","title":"The MergerFS Pool","text":"<p>Finally, we define the virtual pool that combines <code>/mnt/disk1</code>, <code>/mnt/disk2</code>, etc., into <code>/mnt/data</code>.</p> <p>Add this line to the end of <code>/etc/fstab</code>:</p> <pre><code># MergerFS Pool\n# Syntax: branches mount_point type options dump pass \n/mnt/disk* /mnt/data fuse.mergerfs defaults,nonempty,allow_other,use_ino,category.create=mfs,minfreespace=10G,fsname=mergerfs 0 0\n</code></pre>"},{"location":"03-storage/#understanding-the-mergerfs-arguments","title":"Understanding the MergerFS Arguments","text":"<ul> <li> <p><code>/mnt/disk*</code>: The source. The asterisk (*) tells MergerFS to include any     folder starting with \"disk\" in that directory. This makes adding     new drives (disk3, disk4) easy later.</p> </li> <li> <p><code>defaults</code>: Standard mount settings.</p> </li> <li> <p><code>allow_other</code>: Crucial for Docker. Allows users other than the one who     mounted the filesystem (root) to access it. Without this, Plex would     not see the files.</p> </li> <li> <p><code>use_ino</code>: Passes the original inode numbers from the physical disks.     Important for hardlinks to work correctly across the pool.</p> </li> <li> <p><code>category.create=mfs</code>: \"Most Free Space\". When a new file is written to the pool,     MergerFS will write it to the physical disk that has the most     available space. This keeps drive usage balanced.</p> </li> <li> <p><code>minfreespace=10G</code>: Prevents a drive from becoming 100% full, which can cause     filesystem corruption or crashes. If a drive has less than 10GB     left, MergerFS moves to the next drive.</p> </li> </ul>"},{"location":"03-storage/#applying-changes","title":"Applying Changes","text":"<p>Once <code>fstab</code> is saved, create the pool directory and mount everything.</p> <pre><code># Create the pool mount point \nsudo mkdir -p /mnt/data\n\n# Mount all filesystems listed in fstab \nsudo mount -a\n\n# Verify the pool size (Should be sum of disk1 + disk2) \ndf -h /mnt/data\n</code></pre>"},{"location":"04-docker/","title":"Docker &amp; Services","text":"<p>We use Docker to containerize applications. This keeps the base OS clean and allows for easy updates and backups. We orchestrate these containers using Docker Compose, which allows us to define the entire server configuration in a single text file.</p>"},{"location":"04-docker/#installation","title":"Installation","text":"<p>We install the official Docker Engine from the Docker repository to ensure we have the latest version (Debian's default repositories are often outdated). The whole process is very well documented in the Docker documentation.</p>"},{"location":"04-docker/#user-permissions","title":"User Permissions","text":"<p>By default, Docker commands require <code>sudo</code>. To fix this, we add our admin user to the <code>docker</code> group.</p>"},{"location":"04-docker/#directory-structure","title":"Directory Structure","text":"<p>We keep all Docker configurations in our home directory. This makes backing up the server configuration easy: just backup this one folder.</p> <pre><code># Create the main docker directory \nmkdir -p ~/docker\n\n# Create a directory for the compose file \ncd ~/docker \ntouch docker-compose.yml\n</code></pre>"},{"location":"04-docker/#the-docker-compose-file","title":"The Docker Compose File","text":"<p>This is the heart of the server. We edit the file:</p> <pre><code>vim ~/docker/docker-compose.yml\n</code></pre> <p>Below is the configuration template optimized for:</p> <ul> <li> <p>Intel i5-12600K: QuickSync Transcoding enabled.</p> </li> <li> <p>Service User: Using UID 13000 as defined in chapter User &amp; Network</p> </li> <li> <p>Hardlinks: Optimized volume mapping for instant moves.</p> </li> <li> <p>Security: Plex gets Read-Only access.</p> </li> </ul> <pre><code>services:\n  # ==========================================\n  # PLEX - Media Server (Intel QuickSync)\n  # ==========================================\n  plex:\n    image: lscr.io/linuxserver/plex:latest\n    container_name: plex\n    network_mode: host\n    environment:\n      - PUID=13000\n      - PGID=13000\n      - TZ=Europe/Zurich\n      - VERSION=docker\n    volumes:\n      - ~/docker/config/plex:/config\n      - /mnt/data/media:/data/media:ro\n      # TRANSCODING: RAM (/dev/shm)\n      - /dev/shm:/transcode\n    devices:\n      - /dev/dri:/dev/dri\n    restart: unless-stopped\n\n  # ==========================================\n  # SONARR HD (1080p)\n  # ==========================================\n  sonarr:\n    image: lscr.io/linuxserver/sonarr:latest\n    container_name: sonarr\n    environment:\n      - PUID=13000\n      - PGID=13000\n      - TZ=Europe/Zurich\n    volumes:\n      - ~/docker/config/sonarr:/config\n      - /mnt/data:/data\n    ports:\n      - 8989:8989\n    restart: unless-stopped\n\n  # ==========================================\n  # SONARR 4K\n  # ==========================================\n  sonarr4k:\n    image: lscr.io/linuxserver/sonarr:latest\n    container_name: sonarr4k\n    environment:\n      - PUID=13000\n      - PGID=13000\n      - TZ=Europe/Zurich\n    volumes:\n      - ~/docker/config/sonarr4k:/config\n      - /mnt/data:/data\n    ports:\n      - 8990:8989\n    restart: unless-stopped\n\n  # ==========================================\n  # RADARR HD (1080p)\n  # ==========================================\n  radarr:\n    image: lscr.io/linuxserver/radarr:latest\n    container_name: radarr\n    environment:\n      - PUID=13000\n      - PGID=13000\n      - TZ=Europe/Zurich\n    volumes:\n      - ~/docker/config/radarr:/config\n      - /mnt/data:/data\n    ports:\n      - 7878:7878\n    restart: unless-stopped\n\n  # ==========================================\n  # RADARR 4K\n  # ==========================================\n  radarr4k:\n    image: lscr.io/linuxserver/radarr:latest\n    container_name: radarr4k\n    environment:\n      - PUID=13000\n      - PGID=13000\n      - TZ=Europe/Zurich\n    volumes:\n      - ~/docker/config/radarr4k:/config\n      - /mnt/data:/data\n    ports:\n      - 7879:7878\n    restart: unless-stopped\n\n  # ==========================================\n  # SEERR\n  # ==========================================\n  seerr:\n    image: ghcr.io/seerr-team/seerr:latest\n    container_name: seerr\n    user: \"13000:13000\"\n    environment:\n      - TZ=Europe/Zurich\n    volumes:\n      - ~/docker/config/seerr:/app/config\n    ports:\n      - 5055:5055\n    restart: unless-stopped\n\n  # ==========================================\n  # NZBGET\n  # ==========================================\n  nzbget:\n    image: lscr.io/linuxserver/nzbget:latest\n    container_name: nzbget\n    environment:\n      - PUID=13000\n      - PGID=13000\n      - TZ=Europe/Zurich\n    volumes:\n      - ~/docker/config/nzbget:/config\n      - /mnt/data:/data\n      - ~/docker/intermediate_downloads:/data/downloads/intermediate\n    ports:\n      - 6789:6789\n    restart: unless-stopped\n\n  # ==========================================\n  # UNPACKERR\n  # ==========================================\n  unpackerr:\n    image: golift/unpackerr:latest\n    container_name: unpackerr\n    environment:\n      - PUID=13000\n      - PGID=13000\n      - TZ=Europe/Zurich\n      - UNPACKERR_INCOMPLETE_DIR=/data/downloads/intermediate\n\n      # --- SONARR HD (Index 0) ---\n      - UN_SONARR_0_URL=http://sonarr:8989\n      - UN_SONARR_0_API_KEY=&lt;your-api-key&gt;\n\n      # --- SONARR 4K (Index 1) ---\n      - UN_SONARR_1_URL=http://sonarr4k:8989\n      - UN_SONARR_1_API_KEY=&lt;your-api-key&gt;\n\n      # --- RADARR HD (Index 0) ---\n      - UN_RADARR_0_URL=http://radarr:7878\n      - UN_RADARR_0_API_KEY=&lt;your-api-key&gt;\n\n      # --- RADARR 4K (Index 1) ---\n      - UN_RADARR_1_URL=http://radarr4k:7878\n      - UN_RADARR_1_API_KEY=&lt;your-api-key&gt;\n\n      - UNPACKERR_DELETE_AFTER_EXTRACT=true\n      - UNPACKERR_WEB_UI_ENABLED=true\n      - UNPACKERR_WEB_UI_PORT=5001\n\n    volumes:\n      - ~/docker/config/unpackerr:/config\n      - /mnt/data:/data\n      - ~/docker/intermediate_downloads:/data/downloads/intermediate\n    restart: unless-stopped\n</code></pre>"},{"location":"04-docker/#hardware-transcoding-intel-quicksync","title":"Hardware Transcoding (Intel QuickSync)","text":"<p>Because we are using an Intel i5-12600K (12th Gen), we need to ensure the container can access the iGPU.</p>"},{"location":"04-docker/#verify-device-existence","title":"Verify Device Existence","text":"<p>Check if the rendering device exists on the host:</p> <pre><code>ls -l /dev/dri\n</code></pre> <p>You should see <code>card0</code> and <code>renderD128</code>.</p>"},{"location":"04-docker/#permissions","title":"Permissions","text":"<p>The <code>plex</code> container needs permission to access this device. Since we pass the device through Docker, this is usually handled automatically, but sometimes the user inside the container needs to be part of the <code>video</code> or <code>render</code> group.  Note: The LinuxServer.io images usually handle this automatically via the device mapping.</p>"},{"location":"04-docker/#managing-the-stack","title":"Managing the Stack","text":"<p>Once the file is saved, we control the services using standard Docker Compose commands.</p>"},{"location":"04-docker/#starting-services","title":"Starting Services","text":"<p>To download images and start everything in the background (detached mode):</p> <pre><code>cd ~/docker \ndocker compose up -d\n</code></pre>"},{"location":"04-docker/#updating-services","title":"Updating Services","text":"<p>To update all containers to the latest version:</p> <pre><code>cd ~/docker \n# 1. Pull latest images\ndocker compose pull\n\n# 2. Recreate containers (only updates changed ones) \ndocker compose up -d\n\n# 3. Remove old unused images \ndocker image prune -f\n</code></pre>"},{"location":"04-docker/#viewing-logs","title":"Viewing Logs","text":"<p>If a service isn't starting, check the logs:</p> <pre><code># View logs for a specific service \ndocker compose logs -f plex\n\n# View logs for all services (Press Ctrl+C to exit) \ndocker compose logs -f\n</code></pre>"},{"location":"04-docker/#casaos","title":"CasaOS","text":"<p>For convenience, we may set up CasaOS. This is very easy and well described on their GitHub.</p>"},{"location":"05-vps/","title":"VPS Gateway","text":""},{"location":"05-vps/#vps-initial-configuration","title":"VPS Initial Configuration","text":"<p>We use a lightweight VPS (1 vCPU, 2GB RAM) running Debian. We utilize the hosting provider's hardware firewall instead of a local firewall (UFW), as it is easier and leaves fewer opportunities for misconfiguration.</p>"},{"location":"05-vps/#ip-forwarding-on-linux","title":"IP Forwarding on Linux","text":"<p>By default, the Linux kernel acts strictly as a host, not a router. This means it instantly drops any network packet that is not specifically addressed to its own IP address.</p> <p>For our VPN Gateway setup, this behavior is a blocker. The VPS needs to act as a bridge:</p> <ul> <li> <p>It receives traffic from the internet (e.g., a request for Plex).</p> </li> <li> <p>It must pass this traffic from the physical network card (<code>eth0</code>) to the     virtual VPN interface (<code>wg0</code>).</p> </li> </ul> <p>Enabling <code>ip_forward</code> instructs the kernel to permit packets to traverse between network interfaces. We use the <code>sysctl.d</code> directory approach for configuration persistence.</p>"},{"location":"05-vps/#server-setup","title":"Server Setup","text":"<pre><code># 1. Update and Install basics \nsudo apt update &amp;&amp; sudo apt upgrade -y\nsudo apt install -y wireguard curl git\n\n# 2. Enable IP Forwarding\n# We create a dedicated config file in /etc/sysctl.d/ instead of editing the main file.\n# This is more robust on minimal Linux images. \necho \"net.ipv4.ip_forward=1\" &gt; /etc/sysctl.d/99-wireguard.conf\n\n# Apply the changes immediately \nsysctl --system\n</code></pre>"},{"location":"05-vps/#wireguard-configuration","title":"WireGuard Configuration","text":"<p>We are using WireGuard to connect our media server with our VPS. There are some guides out there that ease the pain when setting up the whole thing - for example one for Ubuntu 20.04 that can be used for other versions and distros as well. WireGuard uses asymmetric cryptography. We must generate key pairs (Private/Public) on both the VPS and the Home Server.</p> <pre><code># Run this on BOTH servers to generate keys: \nwg genkey | tee privatekey | wg pubkey &gt; publickey\n\n# Display keys for configuration (Copy these securely!) \ncat privatekey\ncat publickey\n</code></pre>"},{"location":"05-vps/#vps-server-config-etcwireguardwg0conf","title":"VPS Server Config (/etc/wireguard/wg0.conf)","text":"<p>On the VPS, we configure the interface and define the Home Server as a Peer.</p> <pre><code>[Interface]\n# Internal Tunnel IP of the VPS\nAddress = 10.10.10.1/24\n# Custom Port defined in Firewall \nListenPort = 51821 \nPrivateKey = &lt;VPS_PRIVATE_KEY&gt;\n\n# IP Masquerading Rules (NAT)\n# These rules ensure traffic leaving the VPS interface (eth0) appears to come from the VPS IP, \\\n# enabling the return path for traffic originating from the Home Server. \nPostUp = iptables -A FORWARD -i %i -j ACCEPT; iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE\nPostDown = iptables -D FORWARD -i %i -j ACCEPT; iptables -t nat -D POSTROUTING -o eth0 -j MASQUERADE\n\n[Peer]\n# The Home Server \nPublicKey = &lt;HOME_PUBLIC_KEY&gt; \n# The single IP allowed for the Home Server \nAllowedIPs = 10.10.10.2/32\n</code></pre> <pre><code># Start the WireGuard Service on VPS \nsystemctl enable wg-quick@wg0\nsystemctl start wg-quick@wg0\n</code></pre>"},{"location":"05-vps/#home-server-client-config","title":"Home Server Client Config","text":"<p>The home server acts as the client. It initiates the connection to the VPS and uses <code>PersistentKeepalive</code> to keep the NAT session open through the home router.</p> <pre><code># 1. Install WireGuard on Home Server \nsudo apt update &amp;&amp; sudo apt install wireguard -y\n\n# 2. Configure the Interface \nsudo vim /etc/wireguard/wg0.conf\n</code></pre> <pre><code>[Interface] \n# The Internal IP of the Home Server\nAddress = 10.10.10.2/24 \nPrivateKey = &lt;HOME_PRIVATE_KEY&gt;\n\n[Peer] # Connection details for the VPS \nPublicKey = &lt;VPS_PUBLIC_KEY&gt; \n# The Public IP of the VPS and the custom port defined earlier \n# Use this port or any other port you defined (keep in mind that a random port is better)\nEndpoint = &lt;VPS_PUBLIC_IP&gt;:51821 \n# Route only traffic destined for the VPS internal IP through the tunnel \nAllowedIPs = 10.10.10.1/32 \n# Critical: Sends a keepalive packet every 25s to prevent the home router \n# from closing the connection due to inactivity.\nPersistentKeepalive = 25\n</code></pre> <pre><code># 3. Start the Tunnel \nsudo systemctl enable wg-quick@wg0\nsudo systemctl start wg-quick@wg0\n\n# 4. Verification \n# Ping the VPS internal IP to confirm connectivity\nping 10.10.10.1\n</code></pre>"},{"location":"05-vps/#reverse-proxy-setup-vps","title":"Reverse Proxy Setup (VPS)","text":"<p>With the tunnel active, we deploy Nginx Proxy Manager (NPM) on the VPS. You can find more information in the NPM documentation. NPM will terminate SSL connections from the internet and route traffic through the WireGuard tunnel to the home server.</p> <pre><code># 1. Install Docker on VPS \ncurl -fsSL https://get.docker.com | sh\n\n# 2. Create Directory and Config \nmkdir -p ~/nginx-proxy &amp;&amp; cd ~/nginx-proxy \nvim docker-compose.yml\n</code></pre> <pre><code>services:\n  app:\n    image: 'jc21/nginx-proxy-manager:latest'\n    restart: unless-stopped\n    ports:\n      - '80:80'     # Public HTTP Port\n      - '443:443'   # Public HTTPS Port\n      - '81:81'     # Admin Web Interface (setup only!)\n    volumes:\n      - ./data:/data\n      - ./letsencrypt:/etc/letsencrypt\n    environment:\n      DISABLE_IPV6: 'true'\n    depends_on:\n      - crowdsec\n</code></pre> <pre><code># 3. Start the Proxy \ndocker compose up -d\n</code></pre>"},{"location":"05-vps/#configuration-steps","title":"Configuration Steps","text":"<ol> <li> <p>DNS Record: Point an A-Record (e.g., <code>plex.yourdomain.com</code>) to the VPS Public IP.</p> </li> <li> <p>Access Admin UI: Navigate to <code>http://&lt;VPS_IP&gt;:81</code> (Default: admin@example.com /     changeme).</p> </li> <li> <p>Create Proxy Host:</p> <ul> <li> <p>Domain Name: plex.yourdomain.com</p> </li> <li> <p>Scheme: http</p> </li> <li> <p>Forward Host: 10.10.10.2 (Home Server VPN IP)</p> </li> <li> <p>Forward Port: 32400</p> </li> <li> <p>Websockets: Enabled (Critical for Plex)</p> </li> </ul> </li> <li> <p>SSL Tab: Request a new Let's Encrypt certificate and enable     \"Force SSL\".</p> </li> </ol>"},{"location":"05-vps/#plex-configuration-home","title":"Plex Configuration (Home)","text":"<p>Finally, instruct Plex to use the new public URL.</p> <ol> <li> <p>Open Plex Settings &gt; Network.</p> </li> <li> <p>Set Custom server access URLs to: <code>https://plex.yourdomain.com:443</code></p> </li> <li> <p>Disable and re-enable Remote Access to force a config refresh.</p> </li> </ol>"},{"location":"05-vps/#security-hardening","title":"Security Hardening","text":"<p>Once the Proxy Host is configured and confirmed working, we must secure the VPS by closing the administration port.</p> <p>We switch the SSL validation method to \"DNS Challenge\". This would allow us to close Port 80 completely, leaving only the encrypted HTTPS port open. Note: We are using Cloudflare here.</p>"},{"location":"05-vps/#cloudflare-api-token","title":"Cloudflare API Token","text":"<ol> <li> <p>Go to Cloudflare Dashboard &gt; My Profile &gt; API Tokens.</p> </li> <li> <p>Create a token using the Edit zone DNS template for your specific domain.</p> </li> <li> <p>Copy the generated Token.</p> </li> </ol>"},{"location":"05-vps/#configure-npm-for-dns-challenge","title":"Configure NPM for DNS Challenge","text":"<p>In Nginx Proxy Manager, edit the Proxy Host:</p> <ul> <li> <p>SSL Tab: Check \"Use a DNS Challenge\".</p> </li> <li> <p>Provider: Cloudflare.</p> </li> <li> <p>Credentials: Replace the token placeholder:</p> <pre><code>dns_cloudflare_api_token = &lt;YOUR_CLOUDFLARE_TOKEN&gt;\n</code></pre> </li> </ul>"},{"location":"05-vps/#final-firewall-rules","title":"Final Firewall Rules","text":"<p>With DNS validation active, we can close Port 80. The only open ingress ports are now for the VPN tunnel and the encrypted HTTPS stream.</p> Port Protocol Status Reason 22 TCP Allow SSH Management 443 TCP Allow HTTPS Streaming 51821 UDP Allow WireGuard Tunnel 80 TCP DENY Not needed (DNS-01 used) 81 TCP DENY Admin UI (Tunnel only)"},{"location":"05-vps/#secure-admin-access-via-ssh-tunnel","title":"Secure Admin Access via SSH Tunnel","text":"<p>To access the Nginx Proxy Manager interface in the future without exposing the port to the internet, we use local port forwarding.</p> <pre><code># Run this on your local machine to create a secure tunnel\n# Syntax: ssh -L &lt;LocalPort&gt;:127.0.0.1:&lt;RemotePort&gt; &lt;User&gt;@&lt;Host&gt; \nssh -L 8888:127.0.0.1:81 debian@&lt;VPS_PUBLIC_IP&gt;\n</code></pre> <p>Now, open your browser and navigate to: http://localhost:8888</p> <p>The traffic is now encrypted inside the SSH session, making it secure even over public WiFi.</p>"},{"location":"05-vps/#advanced-security-crowdsec-ips","title":"Advanced Security: CrowdSec (IPS)","text":"<p>Since we bypass Cloudflare's proxy (Orange Cloud) to allow direct streaming, the VPS IP address is exposed to the public internet. To protect the server against scanners, brute-force attacks, and botnets, we implement CrowdSec.</p>"},{"location":"05-vps/#what-is-crowdsec","title":"What is CrowdSec?","text":"<p>CrowdSec is a modern, open-source Intrusion Prevention System (IPS). Unlike legacy tools like Fail2Ban, it works on a collaborative model:</p> <ol> <li> <p>Detect (The Agent): It analyzes log files (Nginx, SSH, System) in real-time to detect aggressive behavior.</p> </li> <li> <p>Protect (The Bouncer): Once a threat is detected, the \"Bouncer\" interacts with the firewall to drop the connection immediately.</p> </li> <li> <p>Share (The Crowd): When your server blocks an IP for malicious activity, this information is shared with the global CrowdSec community. Conversely, your server automatically receives a blacklist of IPs that have attacked other users, blocking them before they even touch your server.</p> </li> </ol>"},{"location":"05-vps/#architecture-setup","title":"Architecture Setup","text":"<p>We use a hybrid setup:</p> <ul> <li> <p>The Agent runs inside a Docker container. It reads the logs.</p> </li> <li> <p>The Bouncer runs directly on the Host System (VPS). It manages the <code>iptables</code> firewall rules.</p> </li> </ul>"},{"location":"05-vps/#1-docker-compose-modification-the-agent","title":"1. Docker Compose Modification (The Agent)","text":"<p>We modify the <code>docker-compose.yml</code> to add CrowdSec. Critical Changes:</p> <ul> <li> <p>We map the host's <code>auth.log</code> to protect SSH.</p> </li> <li> <p>We expose Port <code>8080</code> to localhost so the Host-Bouncer can talk to the Docker-Agent.</p> </li> </ul> <pre><code>services:\n  app:\n    image: 'jc21/nginx-proxy-manager:latest'\n    restart: unless-stopped\n    ports:\n      # Public HTTP Port\n      - '80:80'\n      # Public HTTPS Port\n      - '443:443'\n      # Admin Web Interface (setup only!)\n      - '81:81'\n    volumes:\n      - ./data:/data\n      - ./letsencrypt:/etc/letsencrypt\n    environment:\n      DISABLE_IPV6: 'true'\n    depends_on:\n      - crowdsec\n\n  crowdsec:\n    image: crowdsecurity/crowdsec:latest\n    restart: unless-stopped\n    environment:\n      # load Nginx and SSHD rules\n      - COLLECTIONS=crowdsecurity/nginx-proxy-manager crowdsecurity/sshd\n    volumes:\n      # Nginx logs\n      - ./data/logs:/var/log/npm-logs:ro\n      # ssh logs of host to container\n      - /var/log/auth.log:/var/log/host-auth.log:ro\n      # CrowdSec data\n      - ./crowdsec-db:/var/lib/crowdsec/data\n      - ./crowdsec-config:/etc/crowdsec\n    ports:\n      - 8080:8080\n    security_opt:\n      - no-new-privileges=true\n</code></pre>"},{"location":"05-vps/#2-log-acquisition-configuration","title":"2. Log Acquisition Configuration","text":"<p>CrowdSec needs to know exactly which files to analyze. We create <code>crowdsec-config/acquis.yaml</code> mapping the Nginx logs and the system authentication logs.</p> <pre><code># --- Nginx proxy manager logs ---\nfilenames:\n  - /var/log/npm-logs/proxy-host-*_access.log\n  - /var/log/npm-logs/proxy-host-*_error.log\n  - /var/log/npm-logs/fallback_access.log\n  - /var/log/npm-logs/fallback_error.log\nlabels:\n  type: nginx-proxy-manager\n\n# --- SSH host logs ---\nfilenames:\n  - /var/log/host-auth.log\nlabels:\n  type: syslog\n</code></pre> <p>Apply the changes by restarting the stack:</p> <pre><code>docker compose up -d \ndocker compose restart crowdsec\n</code></pre>"},{"location":"05-vps/#3-installing-the-enforcer-firewall-bouncer","title":"3. Installing the Enforcer (Firewall Bouncer)","text":"<p>The Docker container detects the attacks, but it cannot control the host's firewall directly. We install the <code>iptables-bouncer</code> on the VPS host system.</p> <pre><code># 1. Add Repository and Install \ncurl -s https://packagecloud.io/install/repositories/crowdsec/crowdsec/script.deb.sh | sudo bash\nsudo apt install crowdsec-firewall-bouncer-iptables -y\n\n# 2. Generate API Key (inside Docker) \n# The bouncer needs this key to authenticate with the Docker agent \ndocker exec -t nginx-proxy-crowdsec-1 cscli bouncers add firewall-bouncer \n# &gt; Copy the API KEY generated here!\n\n# 3. Configure Bouncer \nsudo vim /etc/crowdsec/bouncers/crowdsec-firewall-bouncer.yaml\n</code></pre> <p>Edit the configuration file to point to the local Docker port:</p> <pre><code>mode: iptables\nupdate_frequency: 10s\nlog_mode: file\nlog_dir: /var/log/\nlog_level: info\n# Point to localhost port 8080 (exposed by Docker)\napi_url: http://127.0.0.1:8080/\n# Paste your generated key here\napi_key: &lt;YOUR_GENERATED_KEY&gt;\ndisable_ipv6: true\n</code></pre> <pre><code># 4. Enable Protection \nsudo systemctl restart crowdsec-firewall-bouncer\n\n# 5. Verify Status\n# Check if the bouncer is registered in the container \ndocker exec -t nginx-proxy-crowdsec-1 cscli bouncers list\n</code></pre>"},{"location":"05-vps/#enrollment-verification","title":"Enrollment &amp; Verification","text":"<p>By default, a fresh CrowdSec installation has an empty blocklist. To immediately receive the full \"Community Blocklist\" (approx. 20,000+ malicious IPs) and visualize attacks, we enroll the instance in the CrowdSec Console.</p> <ol> <li> <p>Go to https://app.crowdsec.net and create a free account.</p> </li> <li> <p>Select \"Enroll my first instance\" to receive an enrollment key.</p> </li> <li> <p>Run the enrollment command inside the Docker container:</p> </li> </ol> <pre><code># Replace &lt;KEY&gt; with the code from the website \ndocker exec -t nginx-proxy-crowdsec-1 cscli console enroll &lt;KEY&gt;\n\n# Restart to force an immediate blocklist update\ndocker compose restart crowdsec\n</code></pre> <p>Verification: The download of the global blocklist (CAPI) takes about 5-10 minutes. Afterward, verify that your server has protected itself against thousands of known bad actors:</p> <pre><code># Check the number of banned IPs from the Community List \ndocker exec -t nginx-proxy-crowdsec-1 cscli decisions list --origin CAPI -a | grep -v \"No decisions\" | wc -l\n</code></pre> <p>Expected Result: A number greater than 15,000. If the result is 0 or 1, wait a few more minutes and try again.</p>"},{"location":"06-energy/","title":"Energy Optimization","text":"<p>Running a server 24/7 impacts the electricity bill. Since a media server stays idle for most of the day, we optimize the hardware to enter low-power states when not in use.</p>"},{"location":"06-energy/#hard-drive-spindown","title":"Hard Drive Spindown","text":"<p>Mechanical hard drives consume approx. 6\u201310 Watts when spinning, but less than 1 Watt in standby. Since we use MergerFS (instead of RAID 5/ZFS), we have a massive advantage: we can spin down drives individually. When watching a movie, only the specific drive containing that file needs to wake up; the others remain asleep.</p>"},{"location":"06-energy/#configuration-via-hdparm","title":"Configuration via hdparm","text":"<p>We use <code>hdparm</code> to set a timeout. If a drive is inactive for a specific time, the motor stops.</p> <pre><code>sudo apt install hdparm -y\n</code></pre> <p>To ensure the configuration persists across reboots and works even if drive letters change (e.g., /dev/sdb becomes /dev/sdc), we identify drives by their UUID.</p> <pre><code># 1. Find the UUIDs of your HDDs (ignore the SSD/Boot drive!) \nlsblk -f\n\n# 2. Edit the configuration \nsudo vim /etc/hdparm.conf\n</code></pre> <p>Add a block for each mechanical HDD at the end of the file:</p> <pre><code># HDD 1\n/dev/disk/by-uuid/YOUR-UUID-HERE {\n    # 241 = 30 min, 242 = 1 hour\n    spindown_time = 241\n    # Aggressive power management\n    apm = 127\n}\n# HDD 2\n/dev/disk/by-uuid/NEXT-UUID-HERE {\n    spindown_time = 241\n    apm = 127\n}\n</code></pre> <p>Note: A spindown time of 30\u201360 minutes is recommended. Setting it too short (e.g., 5 mins) causes frequent start-stop cycles, which wears out the drive mechanics.</p>"},{"location":"06-energy/#verification","title":"Verification","text":"<p>To check if your drives are actually sleeping without waking them up:</p> <pre><code>sudo hdparm -C /dev/sd?\n</code></pre> <p>Output:</p> <ul> <li> <p><code>drive state is: active/idle</code> -&gt; Drive is spinning (consuming power).</p> </li> <li> <p><code>drive state is: standby</code> -&gt; Drive is sleeping (saving power).</p> </li> </ul>"},{"location":"07-backups/","title":"Backups","text":"<p>The media server configuration lives in a single folder (<code>~/docker</code>), which contains the compose file and the <code>config</code> directory for every service. Backing up this folder is enough to restore the whole stack.</p> <p>The main risk is database corruption. Services like Sonarr and Radarr use SQLite, and copying a live database can produce a broken backup. To avoid this we stop the containers, take a consistent snapshot, start the containers again, and only then compress the snapshot to the NAS.</p>"},{"location":"07-backups/#1-persistent-nas-mount","title":"1. Persistent NAS Mount","text":"<p>To make sure the backup target is always available, we mount a NAS share over CIFS at boot.</p>"},{"location":"07-backups/#credentials-file","title":"Credentials File","text":"<p>Store the NAS login in a root-owned file so the password is not written into <code>/etc/fstab</code>.</p> <pre><code>sudo vim /etc/.diskstation_creds\n</code></pre> <pre><code>username=&lt;nas-user&gt;\npassword=&lt;nas-password&gt;\n</code></pre> <p>Lock down the permissions so only root can read it:</p> <pre><code>sudo chown root:root /etc/.diskstation_creds\nsudo chmod 600 /etc/.diskstation_creds\n</code></pre>"},{"location":"07-backups/#fstab-entry","title":"fstab Entry","text":"<p>Add the mount to <code>/etc/fstab</code>:</p> <pre><code>//192.168.x.x/&lt;share&gt; /mnt/docker_backups cifs credentials=/etc/.diskstation_creds,uid=1000,gid=1000,dir_mode=0755,file_mode=0644,nofail,_netdev,vers=3.0 0 0\n</code></pre> <p>Key options:</p> <ul> <li> <p><code>nofail</code>: the boot continues even if the NAS is offline.</p> </li> <li> <p><code>_netdev</code>: waits for the network before mounting.</p> </li> <li> <p><code>vers=3.0</code>: pins the SMB protocol version to avoid negotiation errors.</p> </li> </ul> <p>Test the mount without rebooting:</p> <pre><code>sudo mkdir -p /mnt/docker_backups\nsudo mount -a\nmountpoint /mnt/docker_backups\n</code></pre>"},{"location":"07-backups/#2-why-the-backup-runs-as-root","title":"2. Why the Backup Runs as Root","text":"<p>The service containers run as UID 13000 (see chapter User &amp; Network). Some applications, Plex in particular, store secrets such as <code>Preferences.xml</code> and API tokens with mode <code>0600</code>, readable only by their owner. A normal admin account cannot read these files even when it is a member of the same group, because the group read bit is not set.</p> <p>For that reason the backup script must run as root. Root ignores permission bits and can read every container's config regardless of which UID owns it. This is the standard approach for backing up Docker volumes.</p>"},{"location":"07-backups/#3-the-backup-script","title":"3. The Backup Script","text":"<p>Create the script in a root-owned location so no unprivileged user can edit a job that runs as root.</p> <pre><code>sudo vim /usr/local/sbin/backup-docker-config.sh\n</code></pre> <pre><code>#!/bin/bash\nset -uo pipefail\nexport PATH=/usr/local/bin:/usr/bin:/bin\n\n# --- CONFIG: adjust to your setup ---\nCOMPOSE_DIR=\"/home/&lt;user&gt;/docker\"          # holds docker-compose.yml + ./config\nBACKUP_DEST=\"/mnt/docker_backups\"          # the CIFS mount\nSTAGING=\"/home/&lt;user&gt;/.backup-staging\"     # local temp, needs room for one config copy\n# ------------------------------------\n\nDATE=$(date +%Y-%m-%d_%H%M)\nARCHIVE_NAME=\"mediaserver_cfg_$DATE.tar.gz\"\nCOMPOSE_FILE=$(ls \"$COMPOSE_DIR\"/docker-compose.y*ml 2&gt;/dev/null | head -1)\n\n# 1. Refuse to run unless the NAS is mounted, otherwise tar would fill the root disk\nif ! mountpoint -q \"$BACKUP_DEST\"; then\n  echo \"ERROR: $BACKUP_DEST is not mounted. Aborting.\"; exit 1\nfi\n\ncd \"$COMPOSE_DIR\" || { echo \"ERROR: cannot cd to $COMPOSE_DIR\"; exit 1; }\n\n# 2. Always bring containers back up, even if the script fails or is killed\nrestart() { echo \"Starting containers...\"; docker compose start; }\ntrap restart EXIT\n\n# 3. Stop containers, snapshot config locally, restart as soon as possible\necho \"Stopping containers...\"\ndocker compose stop\n\necho \"Snapshotting config to local staging...\"\nmkdir -p \"$STAGING\"\nrsync -a --delete \"$COMPOSE_DIR/config/\" \"$STAGING/config/\"\ncp \"$COMPOSE_FILE\" \"$STAGING/\"\n\necho \"Restarting containers (downtime ends here)...\"\ndocker compose start\ntrap - EXIT   # already restarted, drop the safety net\n\n# 4. Compress the local snapshot to the NAS while containers run\necho \"Archiving to $BACKUP_DEST/$ARCHIVE_NAME ...\"\nif tar -czf \"$BACKUP_DEST/$ARCHIVE_NAME\" -C \"$STAGING\" config \"$(basename \"$COMPOSE_FILE\")\"; then\n  echo \"Archive OK.\"\n  # 5. Prune old backups only after a successful new one\n  find \"$BACKUP_DEST\" -name \"mediaserver_cfg_*.tar.gz\" -type f -mtime +30 -delete\nelse\n  echo \"ERROR: tar failed, keeping old backups.\"\nfi\n\necho \"Done: $ARCHIVE_NAME\"\n</code></pre> <p>What the script does:</p> <ul> <li> <p>Copies to local staging first, so the containers are only stopped for   the duration of an incremental rsync (seconds after the first run).</p> </li> <li> <p>Uses a <code>trap</code> so the containers always restart, even on error or if the   script is interrupted during the stopped window.</p> </li> <li> <p>Prunes backups older than 30 days, but only after a new archive was   written successfully.</p> </li> </ul> <p>Make the script root-owned and executable:</p> <p><pre><code>sudo chown root:root /usr/local/sbin/backup-docker-config.sh\nsudo chmod 755 /usr/local/sbin/backup-docker-config.sh\n</code></pre> Note that the script can alternatively be located in the user's home directory, but a root-owned directory is preferable.</p>"},{"location":"07-backups/#4-manual-test","title":"4. Manual Test","text":"<p>Run the script once by hand before scheduling it:</p> <pre><code>sudo /usr/local/sbin/backup-docker-config.sh\n</code></pre> <p>Confirm the archive was written and is readable:</p> <pre><code>sudo ls -lh /mnt/docker_backups/\nsudo tar -tzf /mnt/docker_backups/mediaserver_cfg_*.tar.gz | head\n</code></pre> <p>While it runs, you can watch the containers stop and restart in a second terminal:</p> <pre><code>watch -n1 'docker compose -f /home/&lt;user&gt;/docker/docker-compose.yml ps'\n</code></pre>"},{"location":"07-backups/#5-schedule-with-cron","title":"5. Schedule with Cron","text":"<p>Because the job runs as root, install it in root's crontab:</p> <pre><code>sudo crontab -e\n</code></pre> <p>Run it daily at 03:00:</p> <pre><code>0 3 * * * /usr/local/sbin/backup-docker-config.sh &gt;&gt; /var/log/backup-docker-config.log 2&gt;&amp;1\n</code></pre> <p>Other useful times:</p> <ul> <li><code>0 3 * * 1</code> -&gt; Mondays 03:00</li> <li><code>0 3 1,15 * *</code> -&gt; twice a month (1st and 15th)</li> <li><code>0 3 1 * *</code> -&gt; monthly, 1st of the month</li> </ul> <p>Notes:</p> <ul> <li> <p>The script sets its own <code>PATH</code>, since cron does not load the normal   environment.</p> </li> <li> <p><code>&gt;&gt;</code> appends to the log so you keep a history instead of overwriting it   each night.</p> </li> <li> <p>After the first scheduled run, check the log with   <code>sudo tail -n 20 /var/log/backup-docker-config.log</code>.</p> </li> </ul>"},{"location":"07-backups/#6-security-notes","title":"6. Security Notes","text":"<ul> <li> <p>A root cron job is only as safe as the script it runs. Keep the script   in a root-owned directory (<code>/usr/local/sbin</code>) so no unprivileged user   can modify a job that runs as root.</p> </li> <li> <p>The first backup copies the full config and can be large. Every run   after that is incremental, so the stopped window stays short.</p> </li> </ul>"},{"location":"08-costs/","title":"Cost Analysis: Self-Hosting vs. Streaming","text":"<p>Is building and running your own media server actually cheaper than simply paying for streaming subscriptions? This chapter documents the real costs of this build and compares them against the streaming services that would be needed to replace it.</p> <p>The reference scenario is a household that wants up to five people streaming simultaneously. All figures are in Swiss Francs (CHF).</p> <p>Pricing date</p> <p>Streaming prices were last verified in July 2026 using current Swiss retail prices. Providers change their plans and prices frequently, so treat these numbers as a snapshot rather than a fixed truth.</p>"},{"location":"08-costs/#self-hosted-media-server","title":"Self-Hosted Media Server","text":""},{"location":"08-costs/#one-time-costs","title":"One-Time Costs","text":"Item Cost (CHF) 2\u00d7 Seagate IronWolf 12 TB (April 2025) 492.00 Seagate IronWolf 4 TB (August 2024) 99.00 DIY media server build (Jan 2026) 577.60 Plex Pass (Lifetime, December 2024 + trial monthly subs from months before) 108.63 English indexer 40.00 Total 1 317.23"},{"location":"08-costs/#recurring-monthly-costs","title":"Recurring Monthly Costs","text":"Item Cost (CHF) German indexer (CHF 15/year) 1.25 Usenet access 5.78 VPS 3.25 Electricity (\u2248 60 W \u2192 31 kWh \u00d7 CHF 0.33/kWh) \u00b9 10.23 Total 20.51 <p>\u00b9 This has been measured over a period of more than a month. During this period the server had few idle time and the measurement can be taken as a worst case estimate.</p>"},{"location":"08-costs/#streaming-alternative","title":"Streaming Alternative","text":"<p>To match the content and the five-simultaneous-stream requirement, the following subscriptions would be needed. Netflix and Disney+ only reach five concurrent streams by adding a paid extra member on top of their four-stream Premium plans; the remaining services top out at three to four streams.</p> Service Plan (for up to 5 simultaneous streams) Monthly (CHF) Netflix Premium (4 streams) + 1 extra member 30.80 Disney+ Premium (4 streams) + 1 extra member 28.80 Prime Video Standard (3 streams) 9.99 Sky Show Premium (4 streams) 17.90 \u00b2 Paramount+ Premium (4 streams) 17.90 Total 105.39 <p>\u00b2 Sky Show Premium drops by CHF 4.00 to CHF 13.90/month after the first six months, bringing the total down to CHF 101.39/month thereafter.</p>"},{"location":"08-costs/#break-even-analysis","title":"Break-Even Analysis","text":"<p>The one-time investment of CHF 1 317.23 pays for itself once the money not spent on streaming exceeds the server's own running costs. How long that takes depends on how many streaming services the server actually replaces.</p> Replaced streaming services Break-even Netflix only \u2248 10.7 years Netflix + Disney+ \u2248 2.8 years All five services \u2248 1.3 years <p>In other words: if the server merely replaces a single Netflix subscription, the savings are marginal. But as a replacement for the full stack of services required to satisfy five simultaneous viewers, it pays for itself in a little over a year.</p>"},{"location":"08-costs/#effective-monthly-cost-by-runtime","title":"Effective Monthly Cost by Runtime","text":"<p>Amortising the one-time investment over the server's lifetime gives its true effective monthly cost (one-time cost spread across the runtime, plus the recurring monthly costs). The longer it runs, the cheaper it gets:</p> Runtime Effective monthly cost (CHF) 2 years 75.39 3 years 57.10 5 years 42.46 7 years 36.19 <p>Even at a pessimistic two-year lifespan, the effective monthly cost stays below the CHF 105.39 streaming bill; over five years it drops to well under half.</p>"},{"location":"08-costs/#additional-comment","title":"Additional Comment","text":"<p>We have to mention here that prices of components and the Plex subscription have changed. The component prices have to be accepted, however the Plex subscription is not needed since there is Jellyfin as an alternative. But what we need to keep in mind is that if you built a server with a good CPU and enough fast Memory there are way more than 5 simultaneous and 4K streams possible as well, these features would all have to be paid additionally when using streaming services. The main invest is your time but once the server is running and automated there is not much for you to do.</p>"},{"location":"09-further-reading/","title":"Further Links &amp; Related Guides","text":"<p>This chapter collects the external resources, guides and references that informed this setup. They are a good starting point if you want to dig deeper into a specific topic or diverge from the choices made in this guide.</p>"},{"location":"09-further-reading/#setup-guides-platforms","title":"Setup Guides &amp; Platforms","text":"<p>Broader guides covering full media server stacks and alternative platforms.</p> <ul> <li>Docker Media Server (SmartHomeBeginner)     \u2014 A comprehensive walkthrough of a Docker-based media server with 20+ apps.</li> <li>Detailed Guide to Setting Up ZFS RAID on Ubuntu     \u2014 An alternative storage approach to the MergerFS pool used in     Storage Setup.</li> <li>CasaOS     \u2014 A simple, elegant open-source Personal Cloud system if you prefer a     turnkey UI over a hand-rolled stack.</li> </ul>"},{"location":"09-further-reading/#arr-apps-automation","title":"Arr Apps &amp; Automation","text":"<p>Reference material for the Arr ecosystem and the tools that glue it together.</p> <ul> <li>TRaSH Guides \u2014 Collection of Custom Formats (Radarr)     \u2014 The de-facto standard for quality profiles and custom formats.</li> <li>TRaSH Guides \u2014 How to set up Language Custom Formats     \u2014 Useful for filtering releases by language.</li> <li>Unpackerr \u2014 Documentation     and Docker Image     \u2014 Automatically extracts archived downloads for the Arr apps.</li> <li>Bazarr (LinuxServer.io)     \u2014 Companion app that manages subtitles for Sonarr and Radarr libraries.</li> </ul>"},{"location":"09-further-reading/#plex","title":"Plex","text":"<p>Configuration, transcoding and troubleshooting resources for Plex.</p> <ul> <li>Using Hardware-Accelerated Streaming (Plex Support)     \u2014 How to enable and verify hardware transcoding.</li> <li>Media Capabilities Supported by Intel Hardware     \u2014 Reference for which codecs your Intel iGPU can decode/encode.</li> <li>Plex Codec Issue (TrueNAS Community)     \u2014 Troubleshooting thread for codec-related transcoding failures.</li> <li>Can't access Plex from the Android app (r/PleX)     \u2014 Fixes for the common \"works in browser, not in app\" problem.</li> </ul>"},{"location":"09-further-reading/#alternative","title":"Alternative","text":"<ul> <li>Jellyfin     \u2014 Does (basically) the same as Plex does but is open source (=&gt; free).</li> </ul>"},{"location":"09-further-reading/#usenet","title":"Usenet","text":"<p>Guides and services for the download side of the stack.</p> <ul> <li>PCJones Usenet-Guide (German)     \u2014 An excellent German-language beginner's guide to the Usenet.</li> <li>NZBGeek     \u2014 A popular NZB indexer.</li> <li>NzbPlanet     \u2014 Another popular indexer which offers lifetime plan, good for english titles.</li> <li>treasure-maps (SceneNZBs)     \u2014 Another NZB indexer, popular choice for german audio.</li> </ul> <p>Common Usenet providers referenced during setup include TweakNews and Fast Usenet.</p>"},{"location":"09-further-reading/#notifications","title":"Notifications","text":"<ul> <li>Gotify     \u2014 A self-hosted server for sending and receiving push notifications,     handy for wiring up alerts from your services.</li> </ul>"},{"location":"09-further-reading/#streaming-technology-further-reading","title":"Streaming Technology (Further Reading)","text":"<p>Background material for understanding how media streaming actually works under the hood.</p> <ul> <li>howvideo.works     \u2014 A concise primer on digital video and streaming.</li> <li>FFmpeg (Trac)     \u2014 Documentation for the tool underlying nearly all transcoding.</li> <li>HTTP Live Streaming (Apple Developer)     \u2014 The HLS specification and developer documentation.</li> <li>HTTP Headers (MDN)     and HTTP Authentication (MDN)     \u2014 References for the HTTP layer used by proxies and APIs.</li> <li>RFC 6749 \u2014 The OAuth 2.0 Authorization Framework     \u2014 The authorization framework behind many service integrations.</li> </ul>"},{"location":"09-further-reading/#legal","title":"Legal","text":"<ul> <li>Swiss Copyright Act (URG), SR 231.1 \u2014 Art. 19 (Fedlex)     \u2014 The Swiss legal basis for private-use copying. Know the rules that     apply in your jurisdiction.</li> </ul>"}]}